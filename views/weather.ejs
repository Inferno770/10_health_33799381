<!doctype html>
<html>
  <head>
    <title>Weather Forecast - <%= appData.appName %></title>
    <link rel="stylesheet" type="text/css" href="/main.css" />
    <style>
      /* Optional extra styling for the weather card */
      .weather-card {
        border: 1px solid #ccc;
        padding: 20px;
        margin-top: 20px;
        background-color: #f9f9f9;
        max-width: 400px;
      }
      .error { color: red; font-weight: bold; }
    </style>
  </head>
  <body>
    <div class="container">
        <h1>Weather Forecast</h1>
        
        <p>Enter a city to check the current weather:</p>
        <form action="/weather" method="GET">
            <input type="text" name="city" placeholder="e.g. London, Paris, New York" required>
            <input type="submit" value="Get Weather">
        </form>

        <% if (error) { %>
            <p class="error"><%= error %></p>
        <% } %>

        <% if (weather) { %>
          <div class="weather-card">
            <h2>Weather in <%= weather.name %>, <%= weather.sys.country %></h2>
            
            <p><strong>Temperature:</strong> <%= weather.main.temp %>°C</p>
            <p><strong>Humidity:</strong> <%= weather.main.humidity %>%</p>
            
            <p><strong>Wind Speed:</strong> <%= weather.wind.speed %> m/s</p>
            <p><strong>Pressure:</strong> <%= weather.main.pressure %> hPa</p>
            <p><strong>Description:</strong> <%= weather.weather[0].description %></p>
          </div>
        <% } %>

        <p><a href="/">Back to Home</a></p>
    </div>
  </body>
</html>